#include <stdio.h>
int main()
{
float fahrenheit;
float celsius;
printf("Enter the Room Temperature in Fahrenheit: ");
scanf("%f",&fahrenheit);
celsius=(5.0/9.0)*(fahrenheit-32.0);
printf("The Room Temperature in Celsius: %.2f",celsius);
return 0;
}
